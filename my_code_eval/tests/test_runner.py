import traceback
from typing import Tuple, List, Dict, Any

def run_tests(code: str, tests: List[Dict[str, Any]]) -> Tuple[bool, str]:
    """
    if success, test the generated code against the test cases and log the results.
    Args:
        code: The generated code to test
        tests: The test cases to run
    Returns:
        (success, error_message) tuple
    """
    pass

# TODO: add tests and implement the test runner
